<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nubipro - <%= user.username %></title>
</head>
<body>
    <% user.Games.forEach(game => { %>
        <img src="<%= game.imageurl %> " alt="placeholder">
        <%= game.title %>
        <button>Play</button>
    <% }) %>
</body>
</html>