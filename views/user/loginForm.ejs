<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - NubiPro</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #1b2838 0%, #171a21 100%);
            color: #ffffff;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: Arial, sans-serif;

        .login-container {
            background: rgba(27, 40, 56, 0.95);
            border-radius: 4px;
            padding: 2rem;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.5);
            width: 100%;
            max-width: 400px;
            margin: 0 auto;
            position: relative;
            overflow: hidden;
        }

        .login-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 2px;
            background: linear-gradient(90deg, #66c0f4 0%, #1b2838 100%);
        }

        .form-control {
            background: rgba(0, 0, 0, 0.3);
            border: 1px solid #375470;
            color: #ffffff;
            transition: all 0.3s ease;
        }

        .form-control:focus {
            background: rgba(0, 0, 0, 0.4);
            border-color: #66c0f4;
            box-shadow: 0 0 8px rgba(102, 192, 244, 0.4);
            color: #ffffff;
        }

        .btn-login {
            background: linear-gradient(to right, #66c0f4, #4b8bb2);
            border: none;
            color: white;
            padding: 10px 20px;
            font-weight: bold;
            transition: all 0.3s ease;
            width: 100%;
            margin-top: 1rem;
        }

        .btn-login:hover {
            background: linear-gradient(to right, #4b8bb2, #66c0f4);
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(102, 192, 244, 0.4);
        }

        .form-check-input {
            background-color: rgba(0, 0, 0, 0.3);
            border-color: #375470;
        }

        .form-check-input:checked {
            background-color: #66c0f4;
            border-color: #66c0f4;
        }

        .form-label {
            color: #c7d5e0;
            font-weight: 500;
        }

        .forgot-password, .register-link {
            color: #66c0f4;
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .forgot-password:hover, .register-link:hover {
            color: #a4d7f5;
            text-decoration: underline;
        }

        .input-group {
            margin-bottom: 1.5rem;
        }

        .login-header {
            text-align: center;
            margin-bottom: 2rem;
            color: #ffffff;
            text-shadow: 0 0 10px rgba(102, 192, 244, 0.3);
        }

        .form-floating label {
            color: #8f98a0;
        }
    }
    </style>
</head>
<body>
    <div class="container">
        <div class="login-container">
                    <div class="login-header">
                        <h2>Welcome Back</h2>
                        <p class="text-white" style="font-weight: 300; text-shadow: 0 0 10px rgba(102, 192, 244, 0.4)">Sign in to NubiPro</p>
                    </div>
                    <% if (error) { %>
                        <div class="alert error-message mb-4" style="background: rgba(198, 34, 34, 0.1); border-left: 4px solid #c62222; padding: 1rem; border-radius: 3px; animation: fadeIn 0.3s ease-in-out;">
                            <div class="d-flex align-items-center">
                                <i class="fas fa-exclamation-circle me-2" style="color: #ff3333;"></i>
                                <span style="color: #ff9999;"><%= error %></span>
                            </div>
                        </div>
                    <% } %>
                    <form action="/user/login" method="POST">
                        <div class="mb-3">
                            <label for="username" class="form-label">Username</label>
                            <input type="text" class="form-control" id="username" name="username" required>
                        </div>
                        
                        <div class="mb-3">
                            <label for="password" class="form-label">Password</label>
                            <input type="password" class="form-control" id="password" name="password" required>
                        </div>

                        <div class="d-flex justify-content-between align-items-center mb-3">
                            <div class="form-check">
                                <input class="form-check-input" type="checkbox" id="remember" name="remember">
                                <label class="form-check-label" for="remember">
                                    Remember me
                                </label>
                            </div>
                            <a href="#" class="forgot-password">Forgot password?</a>
                        </div>

                        <button type="submit" class="btn btn-login">Sign In</button>

                        <div class="text-center mt-3">
                            <span class="text-muted">New to NubiPro? </span>
                            <a href="/user/register" class="register-link">Create an account</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

